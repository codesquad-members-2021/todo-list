import Icon from '../utilComponent/Icon.js'

const HeaderTitle = function () {
    return (
        <header>
            <h1>TO-DO LIST</h1>
            <Icon />
        </header>
    );
}

export default HeaderTitle;