import styled from 'styled-components';

const CardAuthorStyle = styled.span`
  font-weight: normal;
  font-size: 12px;
  line-height: 17px;

  width: 252px;
  height: auto;

  color: #bdbdbd;

  margin: 8px 0px;
`;

export default CardAuthorStyle;
