import { useRef, useState } from "react";
import styled from "styled-components";

function ColumnTitleInput() {
  return <ColumnTitleBlock></ColumnTitleBlock>;
}

const ColumnTitleBlock = styled.input`
  border: none;
`;
